package its_meow.claimit.api.userconfig;

public class UserConfigFloat extends UserConfig<Float> {

	public UserConfigFloat(String parsedName, float defaultValue, String helpInfo) {
		super(parsedName, defaultValue, helpInfo);
	}

}
