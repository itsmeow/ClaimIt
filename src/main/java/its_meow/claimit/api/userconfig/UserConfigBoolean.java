package its_meow.claimit.api.userconfig;

public class UserConfigBoolean extends UserConfig<Boolean> {

	public UserConfigBoolean(String parsedName, boolean defaultValue, String helpInfo) {
		super(parsedName, defaultValue, helpInfo);
	}

}
