package its_meow.claimit.api.userconfig;

public class UserConfigString extends UserConfig<String> {

	public UserConfigString(String parsedName, String defaultValue, String helpInfo) {
		super(parsedName, defaultValue, helpInfo);
	}

}
